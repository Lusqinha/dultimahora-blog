<div id="<%= dom_id post %>" class="my-10 mb-24 space-y-5 mx-auto post-content text-white">
  <div class="flex md:mx-10 flex-col border-b-2 border-[#2d2d2d] md:flex-row">    
    <div class="flex gap-3 mb-5">
        <%= image_tag "avatar.jpeg", class: "rounded-full w-12 sm:w-20" %>
      <div class="flex flex-col justify-center ">
        <span class="font-bold text-xl"><%= post.user.name %></span>
        <span class="text-gray-500"><%= time_ago_in_words(post.created_at).capitalize %> atrás</span>
      </div>
    </div> 
  </div>
  <div class="md:px-10 text-lg sm:text-xl">
    <h2 class="font-extrabold text-left md:text-left text-3xl md:text-4xl py-2 mb-5 pl-3 border-l-8 border-[#ffbf00] "><%= link_to post.title, post_path(post) %></h2>
    <div class="text-justify px-3">
      <%= @post.content %>
    </div>
  </div>
</div>