<div id="<%= dom_id post %>" class="my-5 mb-24 space-y-5 mx-auto post-content">
    <div class="flex justify-between items-center flex-col">
        <h2 class="font-bold text-5xl"><%= link_to post.title, post_path(post) %></h2>
        <span class="text-gray-500"><%= time_ago_in_words(post.created_at) %> atrás</span>
    </div>
    <p class="mt-3"><%= simple_format(post.content) %></p>
</div>
